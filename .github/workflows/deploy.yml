name: deploy
on:
  push:
    branches: [express]
jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Pull latest code in deployment directory
        run: |
          cd /opt/moftaked/api
          git pull origin express
      - name: Rebuild and restart API container
        run: |
          docker compose -f /opt/moftaked/docker/docker-compose.prod.yml up -d --build api
